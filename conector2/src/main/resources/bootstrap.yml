spring:
  application:
    name: conector2
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false    
      discovery:
        enabled: true
        service-id: config-server 
