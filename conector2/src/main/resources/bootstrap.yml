#server:
#  port: 8085
#spring:
#  application:
#    name: conector2
#  cloud:
#    config:
#      uri: http://localhost:8888
#      fail-fast: false    
#      discovery:
#        enabled: true
#        service-id: config-server
#        
#    stream:
#      bindings:
#        input:
#          destination: transformacionMessages
#          group: conector2IntakeGroup
          
          
server:
  port: 8092
spring:
  application:
    name: conector2
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false    
      discovery:
        enabled: true
        service-id: config-server
        
    stream:
      bindings:
        input:
          destination: transformacionMessagesS2
          group: conector2IntakeGroupS2        

